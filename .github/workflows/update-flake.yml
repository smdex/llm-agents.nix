name: Update Flake
on:
  workflow_call:
    inputs:
      packages:
        description: 'Space-separated list of specific packages to update (empty for all)'
        required: false
        type: string
        default: ''
      inputs:
        description: 'Space-separated list of specific inputs to update (empty for all)'
        required: false
        type: string
        default: ''
      include-inputs:
        description: 'Include flake inputs in the update matrix'
        required: false
        type: boolean
        default: true
      added-packages-only:
        description: 'Only update packages that are not present in upstream/main'
        required: false
        type: boolean
        default: false
      upstream-repository:
        description: 'Upstream owner/repository used to detect fork-added packages'
        required: false
        type: string
        default: 'numtide/llm-agents.nix'
      pr-labels:
        description: 'Comma-separated list of labels to add to PRs'
        required: false
        type: string
        default: 'dependencies,automated'
      auto-merge:
        description: 'Enable auto-merge for created pull requests'
        required: false
        type: boolean
        default: false
    secrets:
      APP_ID:
        description: 'GitHub App ID'
        required: false
      CLIENT_ID:
        description: 'Compatibility fallback for repositories that stored the GitHub App ID in CLIENT_ID'
        required: false
      APP_PRIVATE_KEY:
        description: 'GitHub App private key'
        required: true
jobs:
  discover:
    runs-on: ubuntu-slim
    outputs:
      matrix: ${{ steps.build-matrix.outputs.matrix }}
      has-updates: ${{ steps.build-matrix.outputs.has-updates }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Fetch upstream package tree
        if: ${{ inputs['added-packages-only'] }}
        env:
          UPSTREAM_REPOSITORY: ${{ inputs['upstream-repository'] }}
        run: |
          git remote add upstream "https://github.com/${UPSTREAM_REPOSITORY}.git" || \
            git remote set-url upstream "https://github.com/${UPSTREAM_REPOSITORY}.git"
          git fetch --depth=1 upstream main
      - name: Setup Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            extra-substituters = https://cache.numtide.com
            extra-trusted-public-keys = niks3.numtide.com-1:DTx8wZduET09hRmMtKdQDxNNthLQETkc/yaX7M4qK0g=
      - name: Build update matrix
        id: build-matrix
        env:
          PACKAGES: "${{ inputs.packages }}"
          INPUTS: "${{ inputs.inputs }}"
          INCLUDE_INPUTS: "${{ inputs['include-inputs'] }}"
          ADDED_PACKAGES_ONLY: "${{ inputs['added-packages-only'] }}"
          UPSTREAM_REF: upstream/main
        run: .github/ci/discovery.py
  update:
    needs: discover
    if: needs.discover.outputs.has-updates == 'true'
    runs-on: ubuntu-slim
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.discover.outputs.matrix) }}
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.APP_ID || secrets.CLIENT_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          permission-contents: write
          permission-pull-requests: write
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          token: ${{ steps.app-token.outputs.token }}
          # Full history needed so we can rebase existing update/* branches onto main.
          fetch-depth: 0
      - name: Setup Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            extra-substituters = https://cache.numtide.com
            extra-trusted-public-keys = niks3.numtide.com-1:DTx8wZduET09hRmMtKdQDxNNthLQETkc/yaX7M4qK0g=
      - name: Set up git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - name: Prepare update branch
        # Reuse the existing update/* branch (if any) so manual fixup commits on the
        # open PR are preserved. Rebase it onto main and let the updater commit on top.
        run: |
          if [[ "${{ matrix.type }}" = package ]]; then
            branch="update/${{ matrix.name }}"
          else
            branch="update-${{ matrix.name }}"
          fi
          git fetch origin main
          if git fetch origin "$branch"; then
            echo "Reusing existing branch $branch"
            git checkout -B "$branch" "origin/$branch"
            if ! git rebase origin/main; then
              echo "::warning::Rebase of $branch onto main failed, discarding stale branch"
              git rebase --abort
              git reset --hard origin/main
            fi
          else
            git checkout -B "$branch" origin/main
          fi
      - name: Perform update
        id: update
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          if [[ "${{ matrix.type }}" = package ]]; then
            # Only load the devshell to update packages
            nix develop -c .github/ci/update.py "${{ matrix.type }}" "${{ matrix.name }}"
          else
            .github/ci/update.py "${{ matrix.type }}" "${{ matrix.name }}"
          fi
      - name: Create pull request
        if: steps.update.outputs.updated == 'true'
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          PR_LABELS: ${{ inputs.pr-labels }}
          AUTO_MERGE: ${{ inputs.auto-merge }}
          CHANGELOG_URL: ${{ steps.update.outputs.changelog }}
        run: |
          .github/ci/create_pr.py \
            "${{ matrix.type }}" \
            "${{ matrix.name }}" \
            "${{ matrix.current_version }}" \
            "${{ steps.update.outputs.new_version }}"
  update-readme:
    needs: [discover, update]
    runs-on: ubuntu-slim
    if: always() && needs.discover.outputs.has-updates == 'true'
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.APP_ID || secrets.CLIENT_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          permission-contents: write
          permission-pull-requests: write
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          token: ${{ steps.app-token.outputs.token }}
          ref: main
      - name: Setup Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            extra-substituters = https://cache.numtide.com
            extra-trusted-public-keys = niks3.numtide.com-1:DTx8wZduET09hRmMtKdQDxNNthLQETkc/yaX7M4qK0g=
      - name: Set up git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - name: Regenerate README
        run: ./scripts/generate-package-docs.py
      - name: Create pull request
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          if git diff --quiet README.md; then
            echo "No README changes"
            exit 0
          fi
          branch="update/readme"
          # Check if branch exists remotely and delete it
          if git ls-remote --exit-code --heads origin "$branch" >/dev/null 2>&1; then
            git push origin --delete "$branch" || true
          fi
          git checkout -b "$branch"
          git add README.md
          git commit -m "README: regenerate package documentation"
          git push -u origin "$branch"
          gh pr create \
            --title "README: regenerate package documentation" \
            --body "Automated README regeneration after package updates." \
            --label "documentation,automated"
  summary:
    needs: [discover, update, update-readme]
    runs-on: ubuntu-slim
    if: always() && needs.discover.outputs.has-updates == 'true'
    steps:
      - name: Generate summary
        run: |-
          {
            echo "## Update Summary"
            echo ""

            # Check overall status
            if [ "${{ contains(needs.update.result, 'failure') }}" = "true" ]; then
              echo "⚠️ Some updates failed. Check individual job logs for details."
            else
              echo "✅ All update jobs completed."
            fi

            echo ""
            echo "Configuration:"
            echo "- Auto-merge: ${{ inputs.auto-merge }}"
            echo "- Include flake inputs: ${{ inputs['include-inputs'] }}"
            echo "- Added packages only: ${{ inputs['added-packages-only'] }}"
          } >> "$GITHUB_STEP_SUMMARY"
